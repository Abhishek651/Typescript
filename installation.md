# Installation

## Setup

```bash
npm init -y
npm install typescript ts-node-dev --save-dev
npx tsc --init
```
# --respawn means restart the Node.js process if it exits unexpectedly.

## Run a file

```bash
npx ts-node-dev --respawn <file>.ts
```
